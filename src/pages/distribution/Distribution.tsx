
import React from "react";
import { Navigate } from "react-router-dom";

const Distribution = () => {
  // Redirect to the overview page
  return <Navigate to="/distribution/social-media" replace />;
};

export default Distribution;
